<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Fragments.PrivateChatRoomFragment">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textCreateYourOwnChatRoom"
        android:layout_marginBottom="@dimen/margin_mine"
        android:text="Create your own chat room:"
        android:textSize="@dimen/text_size" />

    <EditText
        android:layout_below="@+id/textCreateYourOwnChatRoom"
        android:id="@+id/messagesRoomNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_mine"
        android:hint="Room Name"
        android:textSize="@dimen/text_size" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/messagesSubmitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@android:drawable/ic_menu_send"
        android:text="Continue"
        android:textSize="@dimen/text_size"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        app:elevation="4dp" />

</RelativeLayout>
